{
	"config": "process.env.USERS_API_AUTH_HEADER = 'X-API-User-Access-Token';\n{{{config}}}",
	"deps": "var users = require(__dirname + '/users');\n{{{deps}}}",
	"models": "models.users = users.model;\n{{{models}}}",
	"middlewares": "{{{middlewares}}}",
	"routes": "app = users.routes(app, express, models);\n{{{routes}}}"
}